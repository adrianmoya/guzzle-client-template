{
    "name": "${service.packagist_name}",
    "type": "library",
    "description": "${service.name}",
    "keywords": ["Guzzle"],
    "authors": [
        {
            "name": "${author.name}",
            "email": "${author.email}"
        }
    ],
    "require": {
        "php": ">=5.3.2",
        "guzzle/guzzle": "*"
    },
    "autoload": {
        "psr-0": { "${service.psr0}": "/src" }
    }
}